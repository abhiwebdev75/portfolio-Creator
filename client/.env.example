# Optional: only needed if the client is deployed on a DIFFERENT origin than the API.
# In local development this is left unset and requests go through the Vite proxy.
# VITE_API_URL=https://your-api-host.com/api
